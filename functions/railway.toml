# Root = `functions`. Link Postgres → DATABASE_URL on the service.
# Migrations run on every deploy/start via `npm start` (prisma migrate deploy && node lib/server.js).

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
restartPolicyType = "ON_FAILURE"
